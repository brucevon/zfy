# 压缩产物部署说明

## 为什么需要压缩产物

博客原始资源由三个文件组成，部署到 Trilium 时需要在分享服务中分别创建并同步三个笔记（模板 / 样式 / 脚本），文件一多部署就容易漏、容易乱：

| 文件 | 说明 | 原始大小 |
|---|---|---|
| `share/blog.ejs` | 模板（页面结构） | ~17.7KB |
| `share/css/blog.css` | 全部样式 | ~88.8KB |
| `share/js/blog.js` | 全部交互脚本 | ~76.6KB |

`share/blog.min.ejs` 将上述三件压缩后内联为**单个文件**，部署时只需一个分享笔记即可完成整站更新，避免遗漏。

## 目录结构

```
项目根目录
├── build-min.js          # 构建脚本（生成压缩产物）
├── share/
│   ├── blog.ejs          # 模板源码（可读，日常修改用）
│   ├── css/blog.css      # 样式源码（可读）
│   ├── js/blog.js        # 脚本源码（可读）
│   └── blog.min.ejs      # ★ 压缩产物（部署用）
```

## 构建步骤

需要 Node.js 环境（≥ 16），首次运行会自动通过 `npx` 拉取 esbuild（需联网）。

```bash
node build-min.js
```

输出示例：

```
已生成 share/blog.min.ejs
  css: 86.7KB -> 62.0KB | js: 74.8KB -> 34.7KB | ejs 合计: 113.5KB
  自检通过 ✓（内联 JS 语法完整）
```

构建完成后 `share/blog.min.ejs` 即为最新部署文件。

## 部署方式

将 `share/blog.min.ejs` 的内容整体替换到 Trilium 中对应的**模板笔记**（原 `blog.ejs` 的分享笔记）即可，无需再维护 `blog.css` / `blog.js` 两个分享笔记。

## 注意事项

1. **日常修改改源码**：样式改 `share/css/blog.css`，脚本改 `share/js/blog.js`，改完重新执行 `node build-min.js`。
2. **勿直接编辑产物**：`blog.min.ejs` 是压缩后的单行内容，不可读，改它没有任何意义（下次构建会被覆盖）。
3. **锚点依赖**：构建脚本通过查找 `blog.ejs` 中的两个锚点完成替换：
   - `<link rel="stylesheet" href="/blog.css" />`
   - `<script src="/blog.js" defer></script>`
   若模板结构变动导致锚点消失，脚本会报错提示。
4. **自动校验**：脚本内置自检，若内联 JS 语法不完整或出现 `</script>` 截断风险会直接中止，不会产出损坏文件。
5. **内联后无独立缓存**：产物内联后浏览器无法单独缓存 CSS/JS，每次页面加载需重新传输（gzip 后约 28KB，可接受）。
