{
  "name": "zfy-blog",
  "version": "0.4.2",
  "private": true,
  "scripts": {
    "build": "node build-min.js",
    "build:clean": "node build-min.js --clean",
    "build:watch": "node build-min.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "html-minifier-terser": "^7.2.0"
  }
}
